function Gameboard() {
    const gameBoard = [];
    const columns = 3;
    const rows = 3;
};

function Player(name, symbol) {
    this.name = name;
    this.symbol = symbol;
};

function gameController() {
    
};